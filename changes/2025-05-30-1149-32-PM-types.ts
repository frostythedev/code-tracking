export type Service = {
    id: number;
    title: string;
    short_desc: string;
    long_desc: string;
    icon: any;
}

export type FAQItem = {
    id: number;
    question: string;
    answer: string;
}